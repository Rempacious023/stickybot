<!DOCTYPE html>
<html>
<head>
    <title>StickyBot Dashboard</title>
    <style>
        body { font-family: Arial; margin: 40px; background: #f5f5f5; }
        form { background: white; padding: 20px; border-radius: 8px; max-width: 600px; }
        label { display: block; margin-top: 15px; font-weight: bold; }
        input, textarea { width: 100%; padding: 10px; margin-top: 5px; border: 1px solid #ddd; border-radius: 4px; box-sizing: border-box; }
        textarea { resize: vertical; min-height: 80px; font-family: monospace; }
        button { background: #4CAF50; color: white; padding: 12px 20px; border: none; border-radius: 4px; cursor: pointer; margin-top: 20px; font-size: 16px; }
        button:hover { background: #45a049; }
        h1 { color: #333; }
    </style>
</head>
<body>
    <h1>StickyBot Configuration Dashboard</h1>
    <form method="POST" action="/update">
        <label>Sticky Message:</label>
        <textarea name="stickyMessage"><%= config.stickyMessage %></textarea>
        
        <label>Tick Interval (ms):</label>
        <input type="number" name="tickInterval" value="<%= config.tickInterval %>" />
        
        <label>Post Interval per Channel (ms):</label>
        <input type="number" name="postInterval" value="<%= config.postInterval %>" />
        
        <label>Channel IDs (separated by commas or spaces):</label>
        <textarea name="channelIDs"><%= config.channelIDs.join(' ') %></textarea>
        
        <button type="submit">Update Configuration</button>
    </form>
</body>
</html>
